<configuration>
  <dsncollection>
    <dsn alias="G2" name="{SCHEMA}" host="{HOST}" port="{PORT}">
    </dsn>
  </dsncollection>
  <databases>
    <database name="{SCHEMA}" host="{HOST}" port="{PORT}">
      <parameter name="CommProtocol" value="TCPIP"/>
    </database>
  </databases>
</configuration> 